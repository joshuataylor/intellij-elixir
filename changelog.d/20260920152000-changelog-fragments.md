---
group: Build / CI
---
**Changelog entries are now written as one file per pull request under `changelog.d/`, and the plugin's change notes are truncated at the descriptor's character cap instead of failing the build.**
